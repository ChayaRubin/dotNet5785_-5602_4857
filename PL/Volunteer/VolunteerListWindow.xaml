<!--<Window
    x:Class="PL.Volunteer.VolunteersListWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:BO="clr-namespace:BO;assembly=BL"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    mc:Ignorable="d"
    Title="Volunteer List"
    Height="500"
    Width="850"
    Background="#E6F0FA"
    WindowStartupLocation="CenterScreen"
    Loaded="Window_Loaded">

    <Window.Resources>

        --><!-- Rounded button style --><!--
        <Style x:Key="RoundedButtonStyle" TargetType="Button">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="10 5"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="BorderThickness" Value="0"/>
        </Style>

        --><!-- Custom ComboBox Style --><!--
        <Style x:Key="ColoredComboBox" TargetType="ComboBox">
            <Setter Property="Foreground" Value="#C25C9E"/>
            <Setter Property="BorderBrush" Value="#C25C9E"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="Padding" Value="4"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <ToggleButton Name="ToggleButton"
                                          IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                          ClickMode="Press"
                                          Background="{TemplateBinding Background}"
                                          BorderBrush="{TemplateBinding BorderBrush}"
                                          BorderThickness="{TemplateBinding BorderThickness}"
                                          Focusable="False"/>
                            <ContentPresenter Name="ContentSite"
                                              IsHitTestVisible="False"
                                              Content="{TemplateBinding SelectionBoxItem}"
                                              ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                              VerticalAlignment="Center"
                                              HorizontalAlignment="Left"
                                              Margin="6,0,25,0"/>
                            <Popup Name="Popup"
                                   Placement="Bottom"
                                   IsOpen="{TemplateBinding IsDropDownOpen}"
                                   AllowsTransparency="True"
                                   Focusable="False"
                                   PopupAnimation="Slide">
                                <Grid Background="{TemplateBinding Background}">
                                    <ScrollViewer>
                                        <StackPanel IsItemsHost="True"/>
                                    </ScrollViewer>
                                </Grid>
                            </Popup>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

    </Window.Resources>

    --><!-- Outer Viewbox for responsiveness --><!--
    <Viewbox Stretch="Uniform">
        <Grid>
            <Border Background="White" CornerRadius="12" Padding="20" BorderBrush="#0F425F" BorderThickness="2" Margin="40">
                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">

                    --><!-- Filter Section --><!--
                    <StackPanel Orientation="Horizontal" Margin="0 0 0 20" HorizontalAlignment="Left">
                        <TextBlock Text="Filter by:" VerticalAlignment="Center" Margin="0 10 10 0"
                                   Foreground="#C25C9E" FontWeight="Bold"/>
                        <ComboBox x:Name="FilterByComboBox"
                                  Width="200"
                                  SelectionChanged="FilterByComboBox_SelectionChanged"
                                  Style="{StaticResource ColoredComboBox}"/>
                    </StackPanel>

                    --><!-- Volunteer List --><!--
                    <ListView x:Name="VolunteersListView" BorderBrush="#B0C4DE" BorderThickness="1" Height="300">
                        <ListView.View>
                            <GridView>
                                <GridViewColumn Header="ID" DisplayMemberBinding="{Binding Id}" Width="100"/>
                                <GridViewColumn Header="Name" DisplayMemberBinding="{Binding FullName}" Width="200"/>
                                <GridViewColumn Header="Is Active" DisplayMemberBinding="{Binding IsActive}" Width="100"/>
                                <GridViewColumn Header="Handled" DisplayMemberBinding="{Binding TotalCallsHandled}" Width="100"/>
                                <GridViewColumn Header="Canceled" DisplayMemberBinding="{Binding TotalCallsCanceled}" Width="100"/>
                                <GridViewColumn Header="Expired" DisplayMemberBinding="{Binding TotalExpiredCalls}" Width="100"/>
                            </GridView>
                        </ListView.View>
                    </ListView>

                    --><!-- Optional Buttons --><!--
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0 20 0 0">
                        <Button Content="Refresh" Click="BtnRefresh_Click" Background="#B9D15F" Style="{StaticResource RoundedButtonStyle}"/>
                        <Button Content="Close" Click="BtnClose_Click" Background="#F9B350" Style="{StaticResource RoundedButtonStyle}"/>
                    </StackPanel>

                </StackPanel>
            </Border>
        </Grid>
    </Viewbox>
</Window>-->
    
<Window
    x:Class="PL.Volunteer.VolunteersListWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:BO="clr-namespace:BO;assembly=BL"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    mc:Ignorable="d"
    Title="Volunteer List"
    Height="500"
    Width="850"
    Background="#E6F0FA"
    WindowStartupLocation="CenterScreen"
    Loaded="Window_Loaded">

    <Window.Resources>

        <!-- Rounded button style -->
        <Style x:Key="RoundedButtonStyle" TargetType="Button">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="10 5"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="BorderThickness" Value="0"/>
        </Style>

        <!-- Custom ComboBox Style -->
        <Style x:Key="ColoredComboBox" TargetType="ComboBox">
            <Setter Property="Foreground" Value="#C25C9E"/>
            <Setter Property="BorderBrush" Value="#C25C9E"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="Padding" Value="4"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>

    </Window.Resources>

    <Viewbox Stretch="Uniform">
        <Grid>
            <Border Background="White" CornerRadius="12" Padding="20" BorderBrush="#0F425F" BorderThickness="2" Margin="40">
                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">

                    <!-- Filter Section -->
                    <StackPanel Orientation="Horizontal" Margin="0 0 0 20" HorizontalAlignment="Left">
                        <TextBlock Text="Filter by Call Type:" VerticalAlignment="Center" Margin="0 10 10 0"
                                   Foreground="#C25C9E" FontWeight="Bold"/>
                        <ComboBox x:Name="FilterByComboBox"
                                  Width="200"
                                  SelectionChanged="FilterByComboBox_SelectionChanged"
                                  Style="{StaticResource ColoredComboBox}"/>
                    </StackPanel>

                    <!-- Volunteer List -->
                    <ListView ItemsSource="{Binding VolunteersListView}" BorderBrush="#B0C4DE" BorderThickness="1" Height="300">
                        <ListView.View>
                            <GridView>
                                <GridViewColumn Header="ID" DisplayMemberBinding="{Binding Id}" Width="100"/>
                                <GridViewColumn Header="Name" DisplayMemberBinding="{Binding FullName}" Width="200"/>
                                <GridViewColumn Header="Is Active" DisplayMemberBinding="{Binding IsActive}" Width="100"/>
                                <GridViewColumn Header="Handled" DisplayMemberBinding="{Binding TotalHandledCalls}" Width="100"/>
                                <GridViewColumn Header="Canceled" DisplayMemberBinding="{Binding TotalCanceledCalls}" Width="100"/>
                                <GridViewColumn Header="Expired" DisplayMemberBinding="{Binding TotalExpiredCalls}" Width="100"/>
                            </GridView>
                        </ListView.View>
                    </ListView>

                    <!-- Buttons -->
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0 20 0 0">
                        <Button Content="Refresh" Click="BtnRefresh_Click" Background="#B9D15F" Style="{StaticResource RoundedButtonStyle}"/>
                        <Button Content="Close" Click="BtnClose_Click" Background="#F9B350" Style="{StaticResource RoundedButtonStyle}"/>
                    </StackPanel>

                </StackPanel>
            </Border>
        </Grid>
    </Viewbox>
</Window>
