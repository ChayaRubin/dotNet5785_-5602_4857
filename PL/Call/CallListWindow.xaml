<Window x:Class="PL.Call.CallListWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:PL.Call"  
        Title="Calls List" Height="600" Width="1000">

    <Window.Resources>
        <!-- Converter -->
        <local:CallTimeLeftConverter x:Key="CallTimeLeftConverter" />
        <!-- Buttons -->
        <Style x:Key="RoundedButtonStyle" TargetType="Button">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="8" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="10 5"/>
            <Setter Property="Margin" Value="5"/>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Filter by type -->
        <StackPanel Orientation="Horizontal" Margin="0 0 0 10">
            <TextBlock Text="Filter by Call Type:" VerticalAlignment="Center" Margin="0 10 5 0"/>
            <ComboBox ItemsSource="{Binding CallTypes}" SelectedItem="{Binding SelectedCallType}" Width="150"/>
        </StackPanel>

        <!-- Calls -->
        <DataGrid Grid.Row="1"
                  ItemsSource="{Binding CallsListView}"
                  SelectedItem="{Binding SelectedCall}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  MouseDoubleClick="CallsListView_MouseDoubleClick">

            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="80"/>
                <DataGridTextColumn Header="Type" Binding="{Binding Type}" Width="120"/>
                <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="100"/>
                <DataGridTextColumn Header="Description" Binding="{Binding Description}" Width="150" />
                <DataGridTextColumn Header="Volunteer" Binding="{Binding LastVolunteerName}" Width="120"/>
                <DataGridTextColumn Header="Total Assignments" Binding="{Binding TotalAssignments}" Width="100"/>
                <DataGridTextColumn Header="Time Left"
                    Binding="{Binding MaxEndTime, Converter={StaticResource CallTimeLeftConverter}}" 
                    Width="120"/>

                <DataGridTextColumn Header="Treatment Duration" Binding="{Binding completeTime}" Width="120"/>

                <DataGridTemplateColumn Width="150">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Button Content="Delete" Click="DeleteCall_Click" CommandParameter="{Binding}" Background="#d87093" Style="{StaticResource RoundedButtonStyle}" />
                                <Button Content="Cancel" Click="CancelAssignment_Click" CommandParameter="{Binding}"  Background="#6495ed" Style="{StaticResource RoundedButtonStyle}"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Bottom Byttons -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10,0,0">
            <Button Content="Add Call" Click="BtnAdd_Click" Background="#6FA8DC" Width="150" Style="{StaticResource RoundedButtonStyle}"/>
            <Button Content="Refresh" Click="BtnRefresh_Click" Background="#B9D15F" Width="80" Style="{StaticResource RoundedButtonStyle}"/>
            <Button Content="Close" Click="BtnClose_Click" Background="#F9B350" Width="80" Style="{StaticResource RoundedButtonStyle}"/>
        </StackPanel>
    </Grid>
</Window>
