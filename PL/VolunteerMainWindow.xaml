<Window x:Class="PL.VolunteerMainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Volunteer Main Window" Height="600" Width="800"
        WindowStartupLocation="CenterScreen">

    <Grid Margin="20">
        <Border CornerRadius="10" BorderBrush="#0F425F" BorderThickness="2" Padding="20">
            <StackPanel>
                <TextBlock Text="{Binding Volunteer.FullName, StringFormat='Welcome, {0}'}"
                           FontSize="18" FontWeight="Bold" Margin="0 0 0 20"/>

                <!-- Volunteer Info -->
                <GroupBox Header="Personal Information" Margin="0 0 0 10">
                    <Grid Margin="10">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="150"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <TextBlock Text="Full Name:" Grid.Row="0" Grid.Column="0"/>
                        <TextBlock Text="{Binding Volunteer.FullName}" Grid.Row="0" Grid.Column="1"/>

                        <TextBlock Text="Phone:" Grid.Row="1" Grid.Column="0"/>
                        <TextBlock Text="{Binding Volunteer.PhoneNumber}" Grid.Row="1" Grid.Column="1"/>

                        <TextBlock Text="Email:" Grid.Row="2" Grid.Column="0"/>
                        <TextBlock Text="{Binding Volunteer.Email}" Grid.Row="2" Grid.Column="1"/>
                    </Grid>
                </GroupBox>

                <!-- Assigned Calls -->
                <GroupBox Header="My Assignments">
                    <ListBox ItemsSource="{Binding VolunteerCalls}" Margin="10">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Border BorderThickness="1" BorderBrush="Gray" Padding="10" Margin="5">
                                    <StackPanel>
                                        <TextBlock Text="{Binding Id, StringFormat='Call ID: {0}'}"/>
                                        <TextBlock Text="{Binding Address, StringFormat='Address: {0}'}"/>
                                        <TextBlock Text="{Binding Status, StringFormat='Status: {0}'}"/>
                                        <TextBlock Text="{Binding Distance, StringFormat='Distance: {0:F1} km'}"/>
                                    </StackPanel>
                                </Border>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </GroupBox>

                <!-- Buttons -->
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0 20 0 0">
                    <Button Content="Update Details" Command="{Binding UpdateCommand}" Width="120" Margin="5"/>
                    <Button Content="Call History" Command="{Binding HistoryCommand}" Width="120" Margin="5"/>
                    <Button Content="Logout" Command="{Binding LogoutCommand}" Width="120" Margin="5"/>
                </StackPanel>
            </StackPanel>
        </Border>
    </Grid>
</Window>
