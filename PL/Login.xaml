<Window x:Class="PL.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:PL"
        xmlns:helpers="clr-namespace:PL"
        Title="Login"
        Height="400" Width="400"
        WindowStartupLocation="CenterScreen"
        Background="#E6F0FA">

    <Window.Resources>
        <Style x:Key="RoundedButtonStyle" TargetType="Button">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="10 5"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Style.Triggers>
                <!-- Property Trigger for Hover -->
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#A34D89"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Validation Style for TextBox -->
        <Style TargetType="TextBox" x:Key="ValidationTextBoxStyle">
            <Style.Triggers>
                <Trigger Property="Text" Value="">
                    <Setter Property="Background" Value="#F8D7DA"/>
                    <Setter Property="ToolTip" Value="This field is required"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
    </Window.Resources>

    <Grid>
        <Border Background="White" CornerRadius="12" Padding="20" BorderBrush="#0F425F" BorderThickness="2" Margin="40">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <TextBlock Text="Welcome to the Volunteer System"
                           FontSize="20" FontWeight="Bold" Foreground="#2798D5"
                           Margin="0 0 0 20" TextAlignment="Center"/>

                <TextBlock Text="Id:" Margin="0 5 0 2"/>
                <TextBox Text="{Binding Id, UpdateSourceTrigger=PropertyChanged}"
                         Style="{StaticResource ValidationTextBoxStyle}"
                         Width="200" Height="30" Margin="0 0 0 10"/>

                <TextBlock Text="Password:" Margin="0 5 0 2"/>
                <PasswordBox Width="200" Height="30" Margin="0 0 0 20"
                             helpers:PasswordBoxHelper.BindPassword="True"
                             helpers:PasswordBoxHelper.BoundPassword="{Binding Password, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>

                <Button Content="Login"
                        Command="{Binding LoginCommand}"
                        Background="#C25C9E"
                        IsDefault="True"
                        Style="{StaticResource RoundedButtonStyle}"/>

                <TextBlock Text="{Binding ErrorMessage}"
                           Foreground="Red" TextAlignment="Center"
                           Margin="10"
                           Visibility="{Binding HasError, Converter={StaticResource BooleanToVisibilityConverter}}"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
